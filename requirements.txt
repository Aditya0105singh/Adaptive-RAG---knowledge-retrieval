# Adaptive RAG — pinned to the exact versions verified in development.
# Note: qdrant-client >= 1.16 removed QdrantClient.search(); the code uses
# query_points() and requires >= 1.16.

# Core framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
streamlit==1.39.0
pydantic==2.9.2
pydantic-settings==2.14.1
python-multipart==0.0.22

# LLM + agent graph
langchain==1.3.8
langchain-community==0.3.31
langchain-groq==1.1.3
langchain-huggingface==1.2.2
langgraph==1.2.4

# Retrieval + storage
qdrant-client==1.17.1
sentence-transformers==5.3.0
pymongo==4.17.0

# Web search
tavily-python==0.7.26

# Document parsing + cost tracking
pypdf==5.9.0
tiktoken==0.13.0

# Observability
structlog==25.5.0
opentelemetry-api==1.42.1
opentelemetry-sdk==1.42.1

# HTTP client (frontend -> backend)
requests==2.34.2
httpx==0.27.2

# Evaluation
ragas==0.2.15
datasets==3.6.0

# Testing
pytest==8.3.3
pytest-asyncio==0.26.0
